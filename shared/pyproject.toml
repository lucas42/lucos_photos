[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "lucos-photos-common"
version = "0.1.0"
dependencies = [
    "sqlalchemy>=2.0",
    "psycopg2-binary",
    "redis",
    "rq",
]
